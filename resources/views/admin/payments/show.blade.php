<x-app-layout><x-slot name="title">{{ $payment->number }}</x-slot><div class="space-y-6"><x-status-alert /><div class="flex justify-between"><div><h2 class="text-xl font-semibold">{{ $payment->number }}</h2><p class="text-sm text-slate-500">{{ $payment->client->displayName() }} · {{ $payment->status }}</p></div><div class="flex gap-2"><a class="rounded-lg border px-3 py-2 text-sm" href="{{ route('admin.payments.receipt',$payment) }}">Ricevuta</a><form method="POST" action="{{ route('admin.payments.paid',$payment) }}">@csrf<button class="rounded-lg bg-slate-950 px-3 py-2 text-sm text-white">Segna pagato</button></form></div></div><section class="rounded-lg border bg-white p-6">Importo: € {{ number_format((float)$payment->amount,2,',','.') }}<br>Pagato: € {{ number_format((float)$payment->paid_amount,2,',','.') }}</section></div></x-app-layout>
