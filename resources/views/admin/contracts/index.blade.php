<x-app-layout>
    <x-slot name="title">Contratti</x-slot>
    <div class="space-y-6"><div class="flex items-center justify-between"><div><h2 class="text-xl font-semibold">Contratti</h2><p class="text-sm text-slate-500">Stati, PDF, invio e firma OTP.</p></div><a class="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white" href="{{ route('admin.contracts.create') }}">Nuovo contratto</a></div><x-status-alert />
        <div class="overflow-hidden rounded-lg border bg-white"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Numero</th><th class="px-5 py-3">Cliente</th><th class="px-5 py-3">Titolo</th><th class="px-5 py-3">Stato</th><th class="px-5 py-3 text-right">Azioni</th></tr></thead><tbody class="divide-y">@forelse($contracts as $contract)<tr><td class="px-5 py-4 font-medium">{{ $contract->number }}</td><td class="px-5 py-4">{{ $contract->client->displayName() }}</td><td class="px-5 py-4">{{ $contract->title }}</td><td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2 py-1 text-xs">{{ $contract->status }}</span></td><td class="px-5 py-4 text-right"><a class="font-medium hover:underline" href="{{ route('admin.contracts.show',$contract) }}">Apri</a></td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">Nessun contratto.</td></tr>@endforelse</tbody></table></div>{{ $contracts->links() }}
    </div>
</x-app-layout>
