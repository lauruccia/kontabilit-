<x-app-layout><x-slot name="title">Template contratti</x-slot><div class="space-y-6"><div class="flex justify-between"><h2 class="text-xl font-semibold">Template contratti</h2><a class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white" href="{{ route('admin.contract-templates.create') }}">Nuovo</a></div><x-status-alert /><div class="grid gap-4">@foreach($templates as $template)<a class="rounded-lg border bg-white p-5 hover:bg-slate-50" href="{{ route('admin.contract-templates.show',$template) }}"><div class="font-semibold">{{ $template->name }}</div><div class="text-sm text-slate-500">{{ $template->category }} · {{ $template->is_active ? 'attivo' : 'non attivo' }}</div></a>@endforeach</div>{{ $templates->links() }}</div></x-app-layout>
