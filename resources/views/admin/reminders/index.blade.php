<x-app-layout><x-slot name="title">Scadenze</x-slot><div class="space-y-6"><div class="flex justify-between"><h2 class="text-xl font-semibold">Scadenze e rinnovi</h2><a class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white" href="{{ route('admin.reminders.create') }}">Nuova</a></div><x-status-alert />@foreach($reminders as $reminder)<div class="flex items-center justify-between rounded-lg border bg-white p-4 text-sm"><div><div class="font-medium">{{ $reminder->title }}</div><div class="text-slate-500">{{ $reminder->due_date->format('d/m/Y') }} · {{ $reminder->type }} · {{ $reminder->status }}</div></div><form method="POST" action="{{ route('admin.reminders.complete',$reminder) }}">@csrf<button class="rounded-lg border px-3 py-2">Completa</button></form></div>@endforeach{{ $reminders->links() }}</div></x-app-layout>
