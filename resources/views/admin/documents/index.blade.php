<x-app-layout><x-slot name="title">Documenti</x-slot><div class="space-y-6"><div class="flex justify-between"><h2 class="text-xl font-semibold">Documenti</h2><a href="{{ route('admin.documents.create') }}" class="rounded-lg bg-slate-950 px-4 py-2 text-sm text-white">Carica</a></div><x-status-alert /><div class="grid gap-3">@foreach($documents as $document)<div class="rounded-lg border bg-white p-4 text-sm"><div class="font-medium">{{ $document->name }}</div><div class="text-slate-500">{{ $document->client?->displayName() ?: 'Nessun cliente' }} · {{ $document->visibility }}</div><a class="underline" href="{{ route('admin.documents.download',$document) }}">Download</a></div>@endforeach</div>{{ $documents->links() }}</div></x-app-layout>
