<!doctype html><html><body style="font-family: DejaVu Sans, sans-serif"><h1>Contratto {{ $contract->number }}</h1><p>Cliente: {{ $contract->client->displayName() }}</p><div style="white-space: pre-line">{{ $contract->rendered_content ?: $contract->terms }}</div>@if($contract->signature)<hr><h3>Firmato elettronicamente tramite OTP</h3><p>Data firma: {{ $contract->signature->signed_at->format('d/m/Y H:i') }}</p><p>IP: {{ $contract->signature->ip_address }}</p><p>Email verificata: {{ $contract->signature->email }}</p><p>Hash documento: {{ $contract->signature->document_hash }}</p>@endif</body></html>
